#include "main.h"
#include <string.h>

/**
 * _strcmp - compares 2 strings.
 * @s1: pointer.
 * @s2: pointer.
 * Return: value 0.
 */

int _strcmp(char *s1, char *s2)
{
	int comp = _strcmp(*s1, *s2)

	return (0);
}
