#include <stdio.h>
/**
 * main - wiriting a program that writes
 * "with propere grammar, but the outcome is a piece of art,"
 * followed by a new line.
 * Return: value 0 (Succes)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
